CONTEXT:
- Endpoint: /search.php
- Injection point: HTML context (h2 tag), unfiltered. Value reflected via POST parameter `searchFor`.

CONFIRMATION:
- Payload: <script>alert(1)</script>
- Filter bypass: None needed (characters `<>"'` are not encoded).

ESCALATION:
- Cookie: <script>alert(document.cookie)</script> (can be used to hijack sessions if an admin visits a page that auto-submits a POST request to this endpoint).
